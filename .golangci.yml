version: "2"

run:
  timeout: 5m

linters:
  default: none
  enable:
    - staticcheck
    - errcheck
    - govet
    - gofmt
    - revive

issues:
  exclude-dirs:
    - vendor
  exclude-rules:
    - linters: [errcheck]
      text: 'error return value not checked.*(Close|Write|Flush)\('
