// Copy to Secrets.xcconfig (gitignored) and fill in your values.
// Never commit Secrets.xcconfig.
//
// BGG_TOKEN — optional. Public BGG collections work without it.
// Obtain from https://boardgamegeek.com/account/api (requires BGG account).
// In CI this is injected automatically from the BGG_TOKEN GitHub secret.
BGG_TOKEN =
