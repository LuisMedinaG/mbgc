# .acai/config.yaml — mbgc acai.sh configuration
# See: https://acai.sh

version: "1.0.0-beta.3"

project:
  name: mbgc
  description: My Board Game Collection — personal board game collection manager with BGG integration

products:
  - name: mbgc
    description: The full MBGC application (web frontend + Go API backend)
