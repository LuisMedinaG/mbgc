# Web frontend — copy to .env and fill in values

# API base URL (no trailing slash)
#   Dev:  leave empty — Vite proxies /api/* → http://localhost:8000
#   Prod: set to gateway URL (injected as Cloudflare Pages env var)
VITE_API_BASE_URL=
