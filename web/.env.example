# Web frontend — copy to .env and fill in values

# API base URL (no trailing slash)
# Dev: leave empty — Vite proxy handles /api/* → http://localhost:8000
# Prod: set to gateway URL (Cloudflare Pages env var)
VITE_API_BASE_URL=
