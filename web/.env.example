# Web frontend — copy to .env.local and fill in values
# Dev:  VITE_API_BASE_URL can stay empty — Vite proxies /api/* → http://localhost:8080
# Prod: injected automatically as a Cloudflare Pages env var (set in Pages dashboard)

VITE_API_BASE_URL=
