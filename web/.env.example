# API base (no trailing slash). Leave empty in dev — Vite proxy handles /api/*.
# In Cloudflare Pages set this to the backend URL, e.g.:
#   VITE_API_BASE_URL=https://myboardgamecollection.fly.dev
VITE_API_BASE_URL=
